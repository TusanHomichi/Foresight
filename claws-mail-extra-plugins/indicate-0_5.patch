diff -r e73fdc6f435e notification_plugin-0.27/configure
--- a/notification_plugin-0.27/configure	Mon Apr 11 12:26:37 2011 +0200
+++ b/notification_plugin-0.27/configure	Mon Apr 11 12:32:53 2011 +0200
@@ -15314,12 +15314,12 @@
     pkg_cv_libindicate_CFLAGS="$libindicate_CFLAGS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"indicate >= \$LIBINDICATE_REQUIRED\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "indicate >= $LIBINDICATE_REQUIRED") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"indicate-0.5 >= \$LIBINDICATE_REQUIRED\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "indicate-0.5 >= $LIBINDICATE_REQUIRED") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_libindicate_CFLAGS=`$PKG_CONFIG --cflags "indicate >= $LIBINDICATE_REQUIRED" 2>/dev/null`
+  pkg_cv_libindicate_CFLAGS=`$PKG_CONFIG --cflags "indicate-0.5 >= $LIBINDICATE_REQUIRED" 2>/dev/null`
 else
   pkg_failed=yes
 fi
@@ -15330,12 +15330,12 @@
     pkg_cv_libindicate_LIBS="$libindicate_LIBS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"indicate >= \$LIBINDICATE_REQUIRED\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "indicate >= $LIBINDICATE_REQUIRED") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"indicate-0.5 >= \$LIBINDICATE_REQUIRED\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "indicate-0.5 >= $LIBINDICATE_REQUIRED") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_libindicate_LIBS=`$PKG_CONFIG --libs "indicate >= $LIBINDICATE_REQUIRED" 2>/dev/null`
+  pkg_cv_libindicate_LIBS=`$PKG_CONFIG --libs "indicate-0.5 >= $LIBINDICATE_REQUIRED" 2>/dev/null`
 else
   pkg_failed=yes
 fi
@@ -15355,9 +15355,9 @@
         _pkg_short_errors_supported=no
 fi
         if test $_pkg_short_errors_supported = yes; then
-	        libindicate_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors "indicate >= $LIBINDICATE_REQUIRED" 2>&1`
+	        libindicate_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors "indicate-0.5 >= $LIBINDICATE_REQUIRED" 2>&1`
         else
-	        libindicate_PKG_ERRORS=`$PKG_CONFIG --print-errors "indicate >= $LIBINDICATE_REQUIRED" 2>&1`
+	        libindicate_PKG_ERRORS=`$PKG_CONFIG --print-errors "indicate-0.5 >= $LIBINDICATE_REQUIRED" 2>&1`
         fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$libindicate_PKG_ERRORS" >&5
